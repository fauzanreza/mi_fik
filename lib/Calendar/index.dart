import 'package:flutter/material.dart';

class CalendarPage extends StatelessWidget {
  const CalendarPage({key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Container()));
  }
}
